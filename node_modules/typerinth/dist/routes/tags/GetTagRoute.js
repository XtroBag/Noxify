"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Route_1 = require("../Route");
const errors_1 = require("../../errors");
class GetTagRoute extends Route_1.Route {
    tagType;
    constructor(baseUrl, ua, cacheManager, tagType) {
        super(baseUrl, ua, cacheManager);
        this.tagType = tagType;
    }
    getCacheKey() {
        return `tags:${this.tagType}`;
    }
    getUrl() {
        return Route_1.Route.addPathSegment(this.baseUrl, `/tag/${this.tagType}`);
    }
    parseData(data) {
        if (!data)
            throw new errors_1.UnexpectedApiError('Unexpected empty response');
        if (data.error)
            throw new errors_1.ApiError(data.error, data.description);
        return data;
    }
}
exports.default = GetTagRoute;
