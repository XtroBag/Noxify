import { URL } from 'url';
import { Route } from '../Route';
import CacheManager from '../../util/CacheManager';
import { TeamMember } from '../../interfaces/teams';
export default class GetProjectTeamMembersRoute extends Route<TeamMember[]> {
    private projectId;
    constructor(baseUrl: URL, ua: string | undefined, cacheManager: CacheManager, authorization: string | undefined, projectId: string);
    getCacheKey(): string | null;
    getUrl(): URL;
    parseData(data: any): TeamMember[];
}
