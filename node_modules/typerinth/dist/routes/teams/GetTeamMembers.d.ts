import { URL } from 'url';
import { Route } from '../Route';
import CacheManager from '../../util/CacheManager';
import { TeamMember } from '../../interfaces/teams';
export default class GetTeamMembersRoute extends Route<TeamMember[]> {
    private teamId;
    constructor(baseUrl: URL, ua: string | undefined, cacheManager: CacheManager, authorization: string | undefined, teamId: string);
    getCacheKey(): string | null;
    getUrl(): URL;
    parseData(data: any): TeamMember[];
}
