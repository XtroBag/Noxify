"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Route_1 = require("../Route");
const errors_1 = require("../../errors");
class GetUserRoute extends Route_1.Route {
    userId;
    constructor(baseUrl, ua, cacheManager, userId) {
        super(baseUrl, ua, cacheManager);
        this.userId = userId;
    }
    getCacheKey() {
        return `user:${this.userId}`;
    }
    getUrl() {
        return Route_1.Route.addPathSegment(this.baseUrl, `/user/${this.userId}`);
    }
    parseData(data) {
        if (data === null)
            throw new errors_1.UserNotFoundError('User not found');
        if (!data)
            throw new errors_1.UnexpectedApiError('Unexpected empty response');
        if (data.error) {
            if (data.error === 'not_found')
                throw new errors_1.UserNotFoundError('User not found');
            throw new errors_1.ApiError(data.error, data.description);
        }
        return data;
    }
}
exports.default = GetUserRoute;
